---
name: walk-procedure
description: "project-os write or rewrite a sitting's procedure playbook. Use when: python3 tools/scripts/walk-sheet.py --check reports that a sitting's procedure no longer covers what the release owes, or reports no procedure for a sitting at all; Before handing over a walk sheet, when the sitting's owed checks have changed since the procedure was written (../release-prep/SKILL.md, step 2a)"
---

<!-- generated by tools/scripts/generate-adapters.py from tools/skills/walk-procedure/SKILL.md; edit the source and re-run, do not edit this file -->

This operation is governed by the canonical project-os playbook.

1. Read `tools/skills/walk-procedure/SKILL.md` in full; it is the source of truth for this skill.
2. Follow its checklist, honouring the preflight and close-out rules in `tools/instructions/LIFECYCLE.md` (document first; update `SNAPSHOT.yaml` and the notes in the same turn as the work). Where the checklist and the repo disagree, say so and file an `ISS-*` rather than improvising.

Use when:

- `python3 tools/scripts/walk-sheet.py --check` reports that a sitting's procedure no longer covers what the release owes, or reports no procedure for a sitting at all.
- Before handing over a walk sheet, when the sitting's owed checks have changed since the procedure was written (`../release-prep/SKILL.md`, step 2a).
